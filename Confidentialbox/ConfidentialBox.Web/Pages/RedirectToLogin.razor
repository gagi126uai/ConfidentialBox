@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        // Redirige apenas inicia el componente
        NavigationManager.NavigateTo("/login", replace: true);
    }
}